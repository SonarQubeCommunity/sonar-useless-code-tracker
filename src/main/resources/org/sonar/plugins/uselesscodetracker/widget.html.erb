<%
  useless_code=measure('total-useless-lines')
  if useless_code
%>
<table width="100%">
  <tbody>
    <tr>
      <td valign="top">
        <div class="dashbox">
          <p class="title"><%= message('widget.useless-code-tracker.name') %></p>
          <p><span class="big"><%= format_measure(useless_code) -%></span><%= tendency_icon('total-useless-lines') -%></p>          
          <p><%= format_measure( 'useless-duplicated-lines', :suffix => ' ' + message('widget.useless-code-tracker.duplicated-lines.suffix'), :url => url_for_drilldown('duplicated_lines'), :default => message('widget.useless-code-tracker.duplicated-lines.default')) -%><%= tendency_icon('useless-duplicated-lines') -%></p>
          <p><%= format_measure( 'dead-code' , :suffix => ' ' + message('widget.useless-code-tracker.dead-code.suffix'), :url => url_for_drilldown('dead-code'), :default => message('widget.useless-code-tracker.dead-code.default')) -%><%= tendency_icon('dead-code') -%></p>
          <p><%= format_measure( 'potential-dead-code', :suffix => ' ' + message('widget.useless-code-tracker.potential-dead-code.suffix'), :url => url_for_drilldown('potential-dead-code'), :default =>  message('widget.useless-code-tracker.potential-dead-code.default')) -%><%= tendency_icon('potential-dead-code') -%></p>
        </div>
      </td>
      <td valign="top">
        <div class="dashbox">
          <p class="title"></p>
        </div>
      </td>
    </tr>
  </tbody>
</table>
<%
  end
%>